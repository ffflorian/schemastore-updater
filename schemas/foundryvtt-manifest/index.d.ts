/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

export type JSONSchemaForFoundryVTTSystemJsonAndModuleJsonFiles = {
  /**
   * The unique package identifier. This must be lowercase and contain no special characters other than hyphens to separate multiple terms. This must also match the name of the directory.
   */
  name: string;
  /**
   * The human readable title of the package which is displayed in the Setup menu and elsewhere.
   */
  title: string;
  /**
   * A description of the package which appears in the Setup menu. May contain HTML tags to structure and style the text.
   */
  description: string;
  /**
   * Specifies the version number as a number or string. Incrementing this will allow Foundry to update the installed package for users and migrate their data if necessary.
   */
  version: number | string;
  /**
   * The person who created the package. Alternatively you may use the `authors` property if multiple people have to be credited.
   */
  author?: string;
  /**
   * Array of authors who have contributed to this package. Cannot be used together with `author`.
   */
  authors?: unknown[];
  /**
   * Specifies the minimum Foundry version required to be able to install and use this package.
   */
  minimumCoreVersion: string;
  /**
   * Specifies the latest Foundry version this package is compatible with.
   */
  compatibleCoreVersion?: string;
  scripts?: StringArray;
  esmodules?: StringArray1;
  styles?: StringArray2;
  /**
   * A list of Compendium packs that are included with the package and should be loaded.
   */
  packs?: {
    /**
     * Name of the Compendium pack. This should be a lowercase string with no special characters other than hyphens.
     */
    name: string;
    /**
     * Human readable label of the Compendium pack. This is displayed in the Compendium sidebar.
     */
    label: string;
    /**
     * The name of the system the Compendium pack is created for. This ensures Foundry knows this Compendium only works for that system. This may be a string or an array of strings.
     */
    system: string | string[];
    /**
     * The name of the module that provides the Compendium pack.
     */
    module?: string;
    /**
     * The relative path to the Compendium .db file. Best practice is to put these in the /packs subdirectory.
     */
    path: string;
    /**
     * Each Compendium pack must designate which type of Entity it contains.
     */
    entity:
      | "Actor"
      | "Item"
      | "Journal"
      | "JournalEntry"
      | "Macro"
      | "Command"
      | "Playlist"
      | "RollTable"
      | "Table"
      | "Scene";
    [k: string]: unknown | undefined;
  }[];
  /**
   * Packages can require other modules, systems, or worlds be installed to allow their use.
   */
  dependencies?: {
    /**
     * Name of the package this package depends on.
     */
    name: string;
    /**
     * The type of dependency.
     */
    type?: "module" | "system" | "world";
    /**
     * Provides an explicit URL to the dependency's manifest to be able to install the package. If not provided, Foundry will check the website package list.
     */
    manifest?: string;
    [k: string]: unknown | undefined;
  }[];
  /**
   * A list of localization files that Foundry can use to translate strings.
   */
  languages?: {
    /**
     * The language code for this definition.
     */
    lang: string;
    /**
     * The formal readable name of the language.
     */
    name: string;
    /**
     * Relative path to the JSON localization file.
     */
    path: string;
    [k: string]: unknown | undefined;
  }[];
  /**
   * A string or array of strings specifying which systems this module is compatible with.
   */
  system?: string | unknown[];
  /**
   * Specifies the default initiative formula if applicable to the system.
   */
  initiative?: string;
  /**
   * Integer specifying the default size of a grid space.
   */
  gridDistance?: number;
  /**
   * A label that specifies the default unit of measure used by the system.
   */
  gridUnits?: string;
  /**
   * An attribute path within the system data model that points to an object that contains both a `value` and a `max` key. The prototype Token will automatically set its primary attribute to this value.
   */
  primaryTokenAttribute?: null | string;
  /**
   * An attribute path within the system data model that points to an object that contains both a `value` and a `max` key. The prototype Token will automatically set its secondary attribute to this value.
   */
  secondaryTokenAttribute?: null | string;
  /**
   * Enables the package to use a custom socket name to send and receive data from clients.
   */
  socket?: boolean;
  /**
   * A public URL that links to the package's website or repository. This will be displayed to allow users to find more information.
   */
  url?: string;
  /**
   * A fixed URL that links to the latest release version of the package. This is used by Foundry to install and update packages.
   */
  manifest?: string;
  /**
   * A public URL pointing to a ZIP file that will be downloaded by Foundry to install or update the package. This can be a Git archive, a file stored in the repository, or a file stored somewhere else.
   */
  download?: string;
  /**
   * Relative path to a file or a URL that points to the package's license.
   */
  license?: string;
  /**
   * Relative path to a file or a URL that points to the package's readme or documentation.
   */
  readme?: string;
  /**
   * A public URL where issues, suggestions, and bugs can be reported by users.
   */
  bugs?: string;
  /**
   * A public URL where update and release notes for the package can be found.
   */
  changelog?: string;
  [k: string]: unknown | undefined;
} & {
  [k: string]: unknown | undefined;
};
/**
 * A list of JavaScript file paths to include when this package is used. Paths are relative to the package root directory. Scripts will be loaded in the listed order as standard scripts.
 */
export type StringArray = string[];
/**
 * A list of JavaScript ES modules to include when this package is used. Paths are relative to the package root directory. Modules will be loaded in the listed order as modules.
 */
export type StringArray1 = string[];
/**
 * A list of CSS file paths to include when this package is used. Paths are relative to the package root directory. Files will be loaded in the listed order.
 */
export type StringArray2 = string[];
