/* tslint:disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

/**
 * defines how the transition happens; you can choose between slide (default), flip, fade, and show.
 */
export type Transition = "slide" | "flip" | "fade" | "show";

/**
 * Schema for UI5/OPENUI5 manifest.json
 */
export interface JSONSchemaForUI5ManifestJsonProjectDeclaration {
  /**
   * Start page of your app, if available
   */
  start_url?: string;
  /**
   * mandatory descriptor format version (not w3c)
   */
  _version?: string;
  "sap.app"?: SAPAppSettings;
  "sap.ui"?: {
    /**
     * Specifies the UI technology; value is UI5
     */
    technology: "UI5";
    /**
     * Contains object with app-specific icons
     */
    icons?: {
      /**
       * Icon of the app
       */
      icon?: string;
      /**
       * ICO file to be used inside the browser and for desktop shortcuts
       */
      favIcon?: string;
      /**
       * 57x57 pixel version for non-retina iPhones
       */
      phone?: string;
      /**
       * 114x114 pixel version for retina iPhones
       */
      "phone@2"?: string;
      /**
       * 72x72 pixel version for non-retina iPads
       */
      tablet?: string;
      /**
       * 144x144 pixel version for retina iPads
       */
      "tablet@2"?: string;
      [k: string]: any;
    };
    /**
     * Contains objects with device types on which the app is running
     */
    deviceTypes: {
      /**
       * Indicator for whether desktop devices are supported
       */
      desktop: boolean;
      /**
       * Indicator for whether tablet devices are supported
       */
      tablet: boolean;
      /**
       * Indicator for whether phone devices are supported
       */
      phone: boolean;
      [k: string]: any;
    };
    /**
     * array of supported SAP themes, such as sap_hcb, sap_belize
     */
    supportedThemes?: any[];
    /**
     * Indicates whether an app shall run in full screen mode
     */
    fullWidth?: boolean;
    [k: string]: any;
  };
  "sap.ui5"?: {
    /**
     * Relative URLs in the component, taking embeddedBy into account if filled, pointing to js (JavaScript) and css resources that are needed by the app for specifying the mandatory uri and an id (optional) for CSS. The JavaScript files are loaded by the require mechanism. The CSS files are added to the head of the HTML page as a link tag. The resources are resolved relative to the location of the manifest.json file.
     */
    resources?: {
      [k: string]: any[];
    };
    /**
     * specifies the external dependencies that are loaded by the OpenUI5 core during the initialization phase of the component and used afterwards, such as libraries or components
     */
    dependencies: {
      /**
       * Minimum version of OpenUI5 that your component requires; this information ensures that the features of the OpenUI5 runtime version of the component are available. As OpenUI5 does not currently enforce use of the correct version, the minUI5Version is used for information purposes only. If the minimum SAPUI5 version criteria is not fulfilled, a warning is issued in the console log.
       */
      minUI5Version: string;
      /**
       * ID (namespace) of the libraries that the OpenUI5 core should load for use in the component.
       */
      libs?: {
        [k: string]: Modules;
      };
      /**
       * ID (namespace) of the libraries that the OpenUI5 core should load for use in the component.
       */
      components?: {
        [k: string]: Modules;
      };
      [k: string]: any;
    };
    /**
     * Defines models that should be created or destroyed along the component's lifecycle. The key represents the model name.
     * Use an empty string ("") for the default model.
     */
    models?: {
      [k: string]: {
        /**
         * Model class name
         */
        type?: string;
        /**
         * Relative URL in the component, taking embeddedBy into account if filled, or server for absolute model
         */
        uri?: string;
        /**
         * String of key or alias from sap.app dataSources to reference an existing data source.
         * The type, uri and settings properties are set according to the data source's type, uri and settings (if not already defined). If the type under sap.app dataSources is OData, an OData Model V2 is created automatically. If you need an OData Model V1, specify the type as well.
         */
        dataSource?: string;
        /**
         * Defines whether or not the model is initialized (preloaded) before the component instance is created and while loading the component preload and its dependencies.
         */
        preload?: boolean;
        /**
         * Additional settings
         */
        settings?: {
          [k: string]: any;
        };
        [k: string]: any;
      };
    };
    /**
     * Specifies the root view that shall be opened; can be the view name as a string for XML views, or the view configuration object with viewName for the view name as a string and type for the type (enumeration of sap.ui.core.mvc.ViewType) and other properties of sap.ui.core.mvc.view.
     */
    rootView?:
      | string
      | {
          [k: string]: any;
        };
    /**
     * Used to enable or disable validation handling by the message manager for this component.
     */
    handleValidation?: boolean;
    /**
     * Static configuration; specify the name-value pairs that you need in your component.
     */
    config?: {
      [k: string]: any;
    };
    routing?: {
      /**
       * Provides configuration parameters for route and router
       */
      config?: {
        /**
         * router class to use
         */
        routerClass?: "sap.ui.core.routing.Router" | "sap.m.routing.Router";
        /**
         * Async routing
         */
        async?: boolean;
        /**
         * Type of the view
         */
        viewType?: "XML" | "JSON" | "JavaScript" | "HTML";
        /**
         * where the view is located in the app (Namespace synthax e.g. 'myapp.views'
         */
        viewPath?: string;
        /**
         * controlId of the control that is used to display the view (e.g. app)
         */
        controlId?: string;
        /**
         * if you have different levels of navigation, especially for flip and slide transitions, where, for example, it is important to know whether the slide animation should go from left to right or vice versa
         */
        viewLevel?: number;
        /**
         * target aggregation to which the view is added. The NavContainer control, for example, has an aggregation called Pages and the shell container has Content
         */
        controlAggregation?: string;
        /**
         * where the control is located
         */
        targetParent?: string;
        transition?: Transition;
        /**
         * Using the bypassed parameter, you specify the navigation target that is used whenever no navigation pattern is matched. If you use this setting, you also have to define a corresponding target in the targets section.
         */
        bypassed?: {
          /**
           * description
           */
          target?: string;
          [k: string]: any;
        };
        [k: string]: any;
      };
      /**
       * Each route defines a name, a pattern, and optionally one or more targets to which to navigate when the route has been matched.
       */
      routes?: {
        /**
         * The name of the route (unique within one router instance)
         */
        name?: string;
        /**
         * The pattern as hash part of the URL that matches the route
         */
        pattern?: string;
        /**
         * The navigation target as defined in the targets section. If you want to load multiple views at the same time, you can assign multiple targets.
         */
        target?: string[];
        /**
         * The parent to specify a route reference in a parent component (in nested components)
         */
        parent?: string;
        [k: string]: any;
      }[];
      /**
       * A target defines the view that is displayed. It is associated with one or more routes or it can be displayed manually from within the app. Whenever a target is displayed, the corresponding view is loaded and added to the aggregation configured with the controlAggregation option of the control.
       */
      targets?: {
        [k: string]: {
          /**
           * Address of the view to be targeted (namespace, e.g. 'myapp.views.myview')
           */
          viewName?: string;
          /**
           * where the view is located in the app
           */
          viewPath?: string;
          /**
           * Id of the view instance
           */
          viewId?: string;
          /**
           * if you have different levels of navigation, especially for flip and slide transitions, where, for example, it is important to know whether the slide animation should go from left to right or vice versa
           */
          viewLevel?: number;
          /**
           * controlId of the control that is used to display the view (e.g. app)
           */
          controlId?: string;
          /**
           * where the control is located
           */
          targetParent?: string;
          /**
           * target aggregation to which the view is added. The NavContainer control, for example, has an aggregation called Pages and the shell container has Content.
           */
          controlAggregation?: string;
          /**
           * clearAggregation specifies whether the aggregation should be cleared before adding the new view instance.
           */
          clearAggregation?: string;
          transition?: Transition;
          /**
           * The view is created and added before the target view is added
           */
          parent?: string;
          [k: string]: any;
        };
      };
      /**
       * The owner parameter defines the owner of all views that are created by the router. This is typically a UIComponent. This parameter is set automatically if the router instance is instantiated by a component.
       */
      owner?: string;
      [k: string]: any;
    };
    extends?: {
      /**
       *  ID (namespace) of the component being extended
       */
      component?: string;
      /**
       * Specifies the minimum version of the component being extended, for information purposes if your app requires a minimum version of the component.
       */
      minVersion?: string;
      /**
       * Component or view extensions, which enable you to replace and extend views and controllers and also to modify the views
       */
      extensions?: {
        [k: string]: any;
      };
      [k: string]: any;
    };
    /**
     * Contains an object with the content density modes that the app supports
     */
    contentDensities: {
      /**
       * indicates whether compact mode is supported
       */
      compact: boolean;
      /**
       * indicates whether cozy mode is supported
       */
      cozy: boolean;
      [k: string]: any;
    };
    /**
     * Map of URL locations keyed by a resource name prefix; only relative paths inside the component are allowed and no ".." characters.
     *
     * This attribute is intended for actual sub-packages of the component only, meaning that it must not be used for the component namespace itself. The defined resource roots will be registered after the component controller is loaded and do not affect the modules being declared as dependencies in the component controller.
     */
    resourceRoots?: {
      /**
       * This interface was referenced by `undefined`'s JSON-Schema definition
       * via the `patternProperty` "^\.{1}\/(\w+\/?)+".
       */
      [k: string]: string;
    };
    /**
     * Name of the component
     */
    componentName?: string;
    /**
     * Enables the auto-prefixing of IDs of ManagedObjects (controls and events) which are created in the context of the createContent function or any other invocation of the Component.prototype.runAsOwner() function
     */
    autoPrefixId?: boolean;
    [k: string]: any;
  };
  /**
   * Specifies the app's URI in the ABAP system, for example /sap/bc/ui5_ui5/sap/appName
   */
  "sap.platform.abap"?: {
    /**
     * uri of the app in the ABAP system
     */
    uri?: string;
    /**
     * represents alternative uri for starting application for legacy apps, such as WDA/SAP GUI, generated by export tool
     */
    uriNwbc?: string;
    [k: string]: any;
  };
  /**
   * HANA-Cloud-Platform-specific attributes
   */
  "sap.platform.hcp"?: {
    [k: string]: any;
  };
  /**
   * in Mobile responsibility
   */
  "sap.mobile"?: {
    [k: string]: any;
  };
  /**
   * in FLP responsibility
   */
  "sap.flp"?: {
    [k: string]: any;
  };
  /**
   * in SAP Fiori responsibility
   */
  "sap.ui.generic.app"?: {
    [k: string]: any;
  };
  /**
   * in Overview Page responsibility
   */
  "sap.ovp"?: {
    [k: string]: any;
  };
  /**
   * in Smart Business responsibility
   */
  "sap.smartbusiness.app"?: {
    [k: string]: any;
  };
  /**
   * in Web Dynpro ABAP responsibility
   */
  "sap.wda"?: {
    [k: string]: any;
  };
  /**
   * in SAP GUI responsibility
   */
  "sap.gui"?: {
    [k: string]: any;
  };
  /**
   * in SAP Cloud Portal responsibility
   */
  "sap.cloud.portal"?: {
    [k: string]: any;
  };
  /**
   * in analysis path framework responsibility
   */
  "sap.apf"?: {
    [k: string]: any;
  };
  [k: string]: any;
}
export interface SAPAppSettings {
  /**
   * SAP app version
   */
  _version?: string;
  /**
   * Mandatory attribute: Unique identifier of the app, which must correspond to the component name
   * The ID must not exceed 70 characters. It must be unique and must correspond to the component name.
   */
  id?: string;
  /**
   * Type of the application
   */
  type?: "application" | "component" | "library";
  /**
   * Relative URL to the properties file that contains the text symbols for the descriptor (The path to the i18n file must not exceed 100 characters)
   */
  i18n?: string;
  /**
   * The version of the application
   */
  applicationVersion: {
    /**
     * The version string
     */
    version?: string;
    [k: string]: any;
  };
  /**
   * Array of relative paths to the nested manifest.json files. Attribute is mandatory if a nested manifest.json exists.
   */
  embeds?: any[];
  /**
   * Relative path back to the manifest.json file of an embedding component or library. Attribute is mandatory for a nested manifest.json.
   */
  embeddedBy?: string;
  /**
   * The entry is language-dependent and specified via {{…}} syntax
   */
  title: string;
  /**
   * Subtitle of your application. Language-dependent entry for a subtitle; specified via {{...}} syntax
   */
  subTitle?: string;
  /**
   * Description for your application. Language-dependent entry that is specified via {{…}} syntax
   */
  description?: string;
  /**
   * Containing an array of language-dependent keywords that are specified via {{…}} syntax
   */
  tags?: {
    /**
     * Array with tag keywords for example 'keywords': ['{{keyWord1}}','{{keyWord2}}']
     */
    keywords: string[];
    [k: string]: any;
  };
  /**
   * Application component hierarchy (SAP's component names for bug reports); attribute is mandatory for SAP apps, but is not used so far for apps developed outside SAP.
   */
  ach?: string;
  /**
   * Unique key/alias for specifying the used data sources
   */
  dataSources?: {
    [k: string]: {
      /**
       * Mandatory relative URL in the component; takes embeddedBy into account, if filled, or the server absolute of the data source, for example "/sap/opu/odata/snce/PO_S_SRV;v=2/"
       */
      uri?: string;
      /**
       * The type of the data source
       */
      type?: "OData" | "ODataAnnotation" | "XML" | "INA" | "JSON";
      /**
       * Data source type-specific attributes
       */
      settings?: {
        /**
         * The version your odata model should use.
         */
        odataVersion?: "2.0" | "4.0";
        /**
         * Relative URL to local metadata document or annotation uri
         */
        localUri?: string;
        /**
         * Array of annotations which references an existing data source of type "ODataAnnotation" under sap.app/dataSources
         */
        annotations?: any[];
        /**
         * Indicates the number of seconds the client is willing to accept with regard to the age of the data that is requested.
         */
        maxAge?: number;
        [k: string]: any;
      };
      /**
       * Array of directly used CDS views. This attribute is optional and only added if used via INA protocol directly, not if used via OData service.
       */
      cdsViews?: any[];
      /**
       * Indicates whether the app is running offline; default is false (online)
       */
      offline?: boolean;
      /**
       * If an app has been generated from a template, this attribute is filled automatically by the generation tool (SAP Web IDE)
       */
      sourceTemplate?: {
        /**
         * Mandatory ID of the template from which the app was generated
         */
        id: string;
        /**
         * Mandatory version of the template from which the app was generated
         */
        version: string;
        /**
         * Indicates if the open source component is part of the app (true) or not (false)
         */
        packagedWithMySelf?: boolean;
        [k: string]: any;
      };
      /**
       * Cross-navigation for specifying inbounds and outbounds
       */
      crossNavigation?: {
        /**
         * Scope of a site
         */
        scopes?: {
          sapSite?: {
            /**
             * unique ID of a site. The scope is set in runtime programmatically or by specific property. Which scope and scope value to set will be detailed in the documentation or applied by a tool.
             */
            value?: string;
            [k: string]: any;
          };
          [k: string]: any;
        };
        /**
         * Unique key or alias to specify inbounds (mandatory)
         */
        inbounds: {
          [k: string]: any;
        };
        [k: string]: any;
      };
      [k: string]: any;
    };
  };
  [k: string]: any;
}
/**
 * This interface was referenced by `undefined`'s JSON-Schema definition
 * via the `patternProperty` "^(\w+\.?)+\w+$".
 *
 * This interface was referenced by `undefined`'s JSON-Schema definition
 * via the `patternProperty` "^(\w+\.?)+\w+$".
 */
export interface Modules {
  /**
   * Minimum version of the module, if your app requires a minimum version
   */
  minVersion?: string;
  /**
   * Specify lazy to indicate that the lib shall be lazy loaded
   */
  lazy?: boolean;
  [k: string]: any;
}
